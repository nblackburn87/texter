<h1>Here are your contacts</h1>

<ul>
  <% @contacts.each do |c| %>
    <li>
        Name:  <%= link_to "#{c.name}", contact_path(c.id) %> <br>
        Phone:  <%= c.number %> <br>
        Email: <%= c.email %> <br><br>
    </li>
  <% end %>
</ul>
